
generator client {
  provider = "prisma-client-js"
  output   = "../generated/3e24db21"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://orderstream:ptiXAKxiCSsOwqH4fVCPkqoQU3BMbmr8@dpg-cldb6d54lnec73e821m0-a.singapore-postgres.render.com/orderstream?schema=3e24db21"
}


model Order {
  id          Int         @id @default(autoincrement())
  table       Int
  product     Int
  productId   Product     @relation(fields: [product], references: [id])
  quantity    Int       
  price       Int       
  provided    Boolean
}

model Product {
  id          Int         @id @default(autoincrement())
  name        String      @unique
  price       Int
  order     Order[]
  log         Log[]
}

model Log {
  id          Int         @id @default(autoincrement())
  table       Int
  product     Int
  productId   Product     @relation(fields: [product], references: [id])
  quantity    Int       
  price       Int       
  timestamp   DateTime    @default(now())
}
